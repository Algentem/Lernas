@import url('https://fonts.googleapis.com/css?family=Roboto:400,700,400italic,700italic&subset=latin');
@import url('https://fonts.googleapis.com/css?family=Lobster Two:400,700,400italic,700italic&subset=latin');

html {
    -webkit-font-smoothing: antialiased;
}

body {
    font-family: 'Roboto','Helvetica Neue',Arial,Helvetica,sans-serif;
    font-size: 16px;
    line-height: 1.5em;
    color: #545454;
    background-color: #ffffff;
}

h1g {
	font-family: 'Roboto', 'Lobster Two', 'Helvetica Neue',Arial,Helvetica,sans-serif;
	font-size: 75px;
}

h1, h2, h3, h4, h5, h6 {
	font-family: 'Roboto', 'Lobster Two', 'Helvetica Neue',Arial,Helvetica,sans-serif;
	color: #222;
	font-weight: 600;
	line-height: 1.3em;
}

p {
    font-family: 'Roboto','Helvetica Neue',Arial,Helvetica,sans-serif;
}

img {
    -webkit-animation: colorize 2s cubic-bezier(0, 0, .78, .36) 1;
    animation: colorize 2s cubic-bezier(0, 0, .78, .36) 1;
    background: transparent;
    border: 10px solid rgba(0, 0, 0, 0.12);
    border-radius: 4px;
    display: block;
    margin: 1.3em auto;
    max-width: 95%;
}

@-webkit-keyframes colorize {
    0% {
        -webkit-filter: grayscale(100%);
    }
    100% {
        -webkit-filter: grayscale(0%);
    }
}

@keyframes colorize {
    0% {
        filter: grayscale(100%);
    }
    100% {
        filter: grayscale(0%);
    }
}

#content {
    max-width: 1000px;
    margin: auto;
    left: 1%;
    right: 1%;
    position: absolute;
}